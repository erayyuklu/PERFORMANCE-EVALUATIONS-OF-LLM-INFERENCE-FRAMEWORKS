apiVersion: v1
kind: Service
metadata:
  name: mock-tools-service
  namespace: mock-tools
  labels:
    app: mock-tools
spec:
  type: ClusterIP
  selector:
    app: mock-tools
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
